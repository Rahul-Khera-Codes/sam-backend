fastapi==0.111.0          # latest: 0.135.1 — upgrade needs starlette compat check (pulls in starlette>=0.38, current pin requires <0.38 — CASA 6.1.1 dependency-scan gap, deferred)
uvicorn[standard]==0.29.0
python-dotenv==1.2.2      # CASA 6.1.1: was 1.0.1 (PYSEC-2026-2270)
pydantic>=2.10,<3
pydantic-settings>=2.6,<3

# Supabase (2.28+ uses realtime 2.x, compatible with websockets>=13 for openai[realtime])
supabase>=2.28.0
python-jose[cryptography]==3.4.0   # CASA 6.1.1: was 3.3.0 (CVE-2024-33663 alg confusion, CVE-2024-33664 JWT-bomb DoS)

# LiveKit (verified: all livekit_service.py imports work on 1.x — agent container runs 1.1.0)
livekit==1.1.2
livekit-api==1.1.0

# OpenAI (>=2.22 + realtime extra for client.realtime.connect WebSocket)
openai[realtime]>=2.22.0

# Token counting for the HR onboarding chat working-memory window (0.13.0+ ships
# prebuilt wheels for Python 3.13, no PyO3 build step needed)
tiktoken>=0.13.0
langsmith>=0.3.16
numpy>=1.24.0
langchain-text-splitters==0.3.11
sentence-transformers==5.1.2
redis==7.0.1
guardrails-ai==0.10.2
# guardrails-ai pins click<=8.2.0, which blocks fixing click's CASA 6.1.1 finding
# (PYSEC-2026-2132) without upgrading guardrails-ai itself — deferred, tracked in
# docs/casa/CASA_REQUIREMENTS_LOG.md.
#
# litellm's CASA 6.1.1 finding (13 CVEs, resolves to 1.83.0 transitively via
# guardrails-ai) is ALSO deferred: every fixed release (1.83.7+) hard-pins
# httpx==0.28.1, which conflicts with this file's httpx==0.27.0 pin below
# (itself shared by fastapi/openai/supabase) — bumping httpx is a coordinated,
# broader change, tracked alongside the fastapi/starlette deferral above.
setuptools>=83.0.0         # CASA 6.1.1: transitive via torch/sentence-transformers, was resolving to 79.0.1 (PYSEC-2026-3447)
# Two more CASA 6.1.1 findings surfaced only after this file's other fixes were
# rebuilt (pinned-floor transitive deps drift forward on every fresh install,
# with no lockfile to keep them reproducible) — both deferred rather than
# force-fixed, since each pulled a cascading incompatible-version chain:
#   - pyasn1 0.4.8 (recursion-DoS, PYSEC-2026-2263, fixed in 0.6.3): blocked by
#     python-jose==3.4.0's hard cap pyasn1<0.5.0. Not considered exploitable
#     here — auth is HS256-only (see auth.py), so the ASN.1/key-parsing code
#     paths pyasn1 backs (RSA/ECDSA key import for google-auth, python-jose's
#     asymmetric algorithms) are never exercised on attacker-controlled input.
#   - httpx2 2.6.0, pulled in by langsmith (request-smuggling + SOCKS5-over-wss
#     plaintext CVEs, fixed in 2.10.0/2.11.0): blocked because the fix requires
#     anyio>=4.10, but anyio==4.3.0 is pinned below for LiveKit/FastAPI/httpx
#     compatibility — bumping anyio is itself a coordinated, broader change.
#     langsmith is only used for observability tracing, not on any
#     request-handling path with attacker-controlled HTTP framing.

# AWS S3 (optional - for recordings if not using Supabase Storage)
boto3==1.34.110

# Google Calendar OAuth (used by integrations router + google_calendar_service)
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.127.0

# HTTP
httpx==0.27.0
pypdf>=5.0.0,<6
pillow>=10.0.0,<12

# Website scraper (knowledge base auto-populate)
beautifulsoup4==4.12.3
defusedxml==0.7.1

# Async
anyio==4.3.0

twilio==9.10.4
stripe>=11.0.0

# Disposable-email-domain blocklist for signup alias blocking. Version tracks the
# underlying domain list (github.com/disposable-email-domains/disposable-email-domains),
# so no upper bound.
disposable-email-domains>=0.0.239

# Scheduler (reminder + reschedule outbound calls cron)
apscheduler>=3.10,<4