fastapi==0.111.0          # latest: 0.135.1 — upgrade needs starlette compat check
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
pydantic>=2.10,<3
pydantic-settings>=2.6,<3

# Supabase (2.28+ uses realtime 2.x, compatible with websockets>=13 for openai[realtime])
supabase>=2.28.0
python-jose[cryptography]==3.3.0

# LiveKit
# PINNED: livekit-api 0.x → 1.x has breaking changes in token + room API.
# Before upgrading verify livekit_service.py imports still work (AccessToken, VideoGrants locations changed).
livekit==0.11.1           # latest: 1.1.2 — DO NOT upgrade without testing livekit_service.py
livekit-api==0.6.0        # latest: 1.1.0 — DO NOT upgrade without testing livekit_service.py

# OpenAI (>=2.22 + realtime extra for client.realtime.connect WebSocket)
openai[realtime]>=2.22.0
numpy>=1.24.0

# AWS S3 (optional - for recordings if not using Supabase Storage)
boto3==1.34.110

# HTTP
httpx==0.27.0

# Async
anyio==4.3.0
